import styled from "styled-components";
import { makeMainContainer } from "../../styles/styles";

export const WrapperStyled = styled.div`
	${makeMainContainer};
	padding-top: 2.5rem;
	width: 100vw;
`;